/*
 * screen folder structure [auth, tabbed, sub-screens / component-screens]
 * font styling and color template
 * font & view components
 * build tab screens
 * bottom tab navigation
 * main sub, profile ... settings sub screens
 * hardcoded data object insertion to all variables
 * unit testing
 * color scheme
 * add icons,images, splashm welcome, login & sign up screens
 * integration testing
 * auth set up, backend, and user match with user data
 * db schema from medlink / templated backend
 * api build
 * api and hosted db data communication / link to firebase / firestore
 * end to end testing
 * internal full app test
 * external (client) testing
 * deployment
 * completion
 */